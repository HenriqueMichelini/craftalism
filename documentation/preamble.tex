% =============================================================================
% preamble.tex — Craftalism Documentation Standard
% Include this file from main.tex with: \input{preamble}
% Do not modify unless updating the project-wide standard.
% =============================================================================

% -----------------------------------------------------------------------------
% Page geometry
% -----------------------------------------------------------------------------
\usepackage[
  a4paper,
  top=2.5cm,
  bottom=2.5cm,
  left=3.0cm,
  right=2.5cm,
  headheight=14pt
]{geometry}

% -----------------------------------------------------------------------------
% Encoding and fonts
% -----------------------------------------------------------------------------
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}           % Improved text justification

% -----------------------------------------------------------------------------
% Color palette
% -----------------------------------------------------------------------------
\usepackage[dvipsnames, table]{xcolor}

\definecolor{CraftBlue}{HTML}{1A3A5C}      % Primary — headings, rules
\definecolor{CraftAccent}{HTML}{2E86AB}    % Accent — links, highlights
\definecolor{CraftGray}{HTML}{5A6475}      % Secondary text
\definecolor{CraftLight}{HTML}{F4F6F8}     % Table row shading
\definecolor{CraftBorder}{HTML}{D0D7DE}    % Table/box borders
\definecolor{CraftCode}{HTML}{F6F8FA}      % Code block background
\definecolor{CraftWarn}{HTML}{FFF8E7}      % Warning box background
\definecolor{CraftWarnBorder}{HTML}{D4A017}% Warning box border
\definecolor{CraftNote}{HTML}{EAF4FB}      % Note box background
\definecolor{CraftNoteBorder}{HTML}{2E86AB}% Note box border

% -----------------------------------------------------------------------------
% Hyperlinks and PDF metadata
% -----------------------------------------------------------------------------
\usepackage[
  colorlinks  = true,
  linkcolor   = CraftAccent,
  urlcolor    = CraftAccent,
  citecolor   = CraftAccent,
  pdftitle    = {Craftalism — Software System Documentation},
  pdfauthor   = {Henrique Michelini},
  pdfsubject  = {Technical Documentation},
  pdfkeywords = {Craftalism, Minecraft, Spring Boot, OAuth2, React}
]{hyperref}

% -----------------------------------------------------------------------------
% Headers and footers
% -----------------------------------------------------------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}

\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{CraftBorder}\leaders\hrule height \headrulewidth\hfill}}
\renewcommand{\footrulewidth}{0.4pt}
\renewcommand{\footrule}{\hbox to\headwidth{\color{CraftBorder}\leaders\hrule height \footrulewidth\hfill}}

\fancyhead[L]{\small\textcolor{CraftGray}{Craftalism — Software System Documentation}}
\fancyhead[R]{\small\textcolor{CraftGray}{\nouppercase{\leftmark}}}
\fancyfoot[C]{\small\textcolor{CraftGray}{\thepage}}

% Plain style for chapter-opening pages
\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[C]{\small\textcolor{CraftGray}{\thepage}}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0.4pt}
  \renewcommand{\footrule}{\hbox to\headwidth{\color{CraftBorder}\leaders\hrule height \footrulewidth\hfill}}
}

% -----------------------------------------------------------------------------
% Chapter and section styling
% -----------------------------------------------------------------------------
\usepackage{titlesec}

% Chapter: large number + title on two lines with a colored rule
\titleformat{\chapter}[display]
  {\normalfont\bfseries\color{CraftBlue}}
  {\large\textcolor{CraftAccent}{Module \thechapter}}
  {0pt}
  {\Huge}
  [\vspace{2pt}{\color{CraftAccent}\titlerule[1.5pt]}]

\titlespacing*{\chapter}{0pt}{-20pt}{24pt}

% Section: bold, colored
\titleformat{\section}
  {\normalfont\Large\bfseries\color{CraftBlue}}
  {\thesection}
  {0.75em}
  {}
  [\vspace{2pt}{\color{CraftBorder}\titlerule[0.5pt]}]

\titlespacing*{\section}{0pt}{18pt}{8pt}

% Subsection: medium, accent color
\titleformat{\subsection}
  {\normalfont\large\bfseries\color{CraftGray}}
  {\thesubsection}
  {0.75em}
  {}

\titlespacing*{\subsection}{0pt}{12pt}{4pt}

% Subsubsection: normal size, no number
\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries\color{CraftGray}}
  {}
  {0em}
  {}

% -----------------------------------------------------------------------------
% Paragraph spacing
% -----------------------------------------------------------------------------
\usepackage[parfill]{parskip}
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

% -----------------------------------------------------------------------------
% Tables
% -----------------------------------------------------------------------------
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{longtable}           % Tables that span multiple pages

% New column type: left-aligned, wrapping, with padding
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\newcolumntype{R}[1]{>{\raggedleft\arraybackslash}p{#1}}

% Alternate row shading — use \rowcolors in table environments
% Example: \rowcolors{2}{CraftLight}{white}

% Shared table style macro
\newcommand{\tableheadstyle}{\rowcolor{CraftBlue}\color{white}\bfseries}

% -----------------------------------------------------------------------------
% Code listings
% -----------------------------------------------------------------------------
\usepackage{listings}

\lstdefinestyle{craftalism}{
  backgroundcolor   = \color{CraftCode},
  basicstyle        = \ttfamily\small,
  breakatwhitespace = false,
  breaklines        = true,
  captionpos        = b,
  commentstyle      = \color{CraftGray}\itshape,
  frame             = single,
  framesep          = 4pt,
  framerule         = 0.4pt,
  rulecolor         = \color{CraftBorder},
  keepspaces        = true,
  keywordstyle      = \color{CraftBlue}\bfseries,
  numbers           = left,
  numbersep         = 10pt,
  numberstyle       = \tiny\color{CraftGray},
  showspaces        = false,
  showstringspaces  = false,
  showtabs          = false,
  stringstyle       = \color{CraftAccent},
  tabsize           = 2,
  xleftmargin       = 16pt,
  xrightmargin      = 4pt,
}

\lstset{style=craftalism}

% Convenience environments for common languages
\lstnewenvironment{javalist}[1][]
  {\lstset{language=Java, #1}}{}

\lstnewenvironment{bashlist}[1][]
  {\lstset{language=bash, #1}}{}

\lstnewenvironment{yamllist}[1][]
  {\lstset{language=, morekeywords={true,false,null}, #1}}{}

% Inline code shortcut: \code{text}
\newcommand{\code}[1]{\texttt{#1}}

% -----------------------------------------------------------------------------
% Info and warning boxes
% -----------------------------------------------------------------------------
\usepackage[framemethod=TikZ]{mdframed}

% Note box (blue)
\newmdenv[
  backgroundcolor = CraftNote,
  linecolor       = CraftNoteBorder,
  linewidth       = 1.5pt,
  leftline        = true,
  topline         = false,
  rightline       = false,
  bottomline      = false,
  innerleftmargin = 10pt,
  innerrightmargin= 8pt,
  innertopmargin  = 8pt,
  innerbottommargin=8pt,
  skipabove       = 8pt,
  skipbelow       = 8pt,
]{notebox}

% Warning box (amber)
\newmdenv[
  backgroundcolor = CraftWarn,
  linecolor       = CraftWarnBorder,
  linewidth       = 1.5pt,
  leftline        = true,
  topline         = false,
  rightline       = false,
  bottomline      = false,
  innerleftmargin = 10pt,
  innerrightmargin= 8pt,
  innertopmargin  = 8pt,
  innerbottommargin=8pt,
  skipabove       = 8pt,
  skipbelow       = 8pt,
]{warnbox}

% Convenience macros
\newcommand{\note}[1]{\begin{notebox}\textbf{Note:} #1\end{notebox}}
\newcommand{\important}[1]{\begin{warnbox}\textbf{Important:} #1\end{warnbox}}

% -----------------------------------------------------------------------------
% Lists
% -----------------------------------------------------------------------------
\usepackage{enumitem}
\setlist[itemize]{
  leftmargin   = 1.5em,
  itemsep      = 3pt,
  parsep       = 0pt,
  topsep       = 4pt,
}
\setlist[enumerate]{
  leftmargin   = 1.5em,
  itemsep      = 3pt,
  parsep       = 0pt,
  topsep       = 4pt,
}

% -----------------------------------------------------------------------------
% Graphics and TikZ
% -----------------------------------------------------------------------------
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, backgrounds}

% Shared TikZ styles for architecture diagrams
\tikzset{
  service/.style = {
    rectangle, rounded corners=4pt,
    draw=CraftBlue, fill=CraftLight,
    text width=3.2cm, minimum height=1cm,
    align=center, font=\small\bfseries,
    text=CraftBlue
  },
  database/.style = {
    cylinder, shape border rotate=90,
    draw=CraftAccent, fill=CraftNote,
    text width=2.4cm, minimum height=1cm,
    align=center, font=\small\bfseries,
    text=CraftBlue
  },
  external/.style = {
    rectangle, rounded corners=4pt,
    draw=CraftGray, fill=white,
    text width=3.0cm, minimum height=0.9cm,
    align=center, font=\small,
    text=CraftGray, dashed
  },
  flow/.style = {
    -Stealth, thick, color=CraftAccent
  },
  flowlabel/.style = {
    font=\scriptsize, color=CraftGray
  },
}

% -----------------------------------------------------------------------------
% Table of contents styling
% -----------------------------------------------------------------------------
\usepackage{titletoc}

\titlecontents{chapter}
  [0em]
  {\vspace{6pt}\bfseries\color{CraftBlue}}
  {\contentslabel{2em}}
  {}
  {\hfill\contentspage}

\titlecontents{section}
  [2em]
  {\small}
  {\contentslabel{2em}}
  {}
  {\titlerule*[6pt]{.}\contentspage}

\titlecontents{subsection}
  [4em]
  {\small\color{CraftGray}}
  {\contentslabel{2.5em}}
  {}
  {\titlerule*[6pt]{.}\contentspage}

% -----------------------------------------------------------------------------
% Miscellaneous utilities
% -----------------------------------------------------------------------------
\usepackage{datetime2}           % Consistent date formatting
\usepackage{amsmath}             % Math (rarely needed, but safe to include)

% Version macro — update in main.tex
\newcommand{\docversion}{1.0}

% Module badge macro — prints colored label before chapter title description
\newcommand{\modulelang}[1]{%
  \noindent
  \colorbox{CraftAccent}{\color{white}\small\bfseries\,#1\,}\quad
}
